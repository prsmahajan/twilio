.env
__pycache__/
*.pyc
*.pyo
venv/
.venv/
frontend/node_modules/
# static/dist IS committed on purpose: PythonAnywhere free accounts have no
# reliable Node toolchain, so the built frontend ships with the repo.
# Rebuild with: cd frontend && npm run build

# local sqlite call log (WAL mode adds -wal/-shm sidecars)
*.db
*.db-wal
*.db-shm
.env.bak
.DS_Store
tests/node_modules/

# local screenshots — some contain Twilio SIDs, tunnel URLs or real numbers.
# Matches ss1.png as well as ss-1.png: the earlier `ss-*.png` let the unhyphenated
# ones through.
ss*.png

# Firecrawl scratch output
.firecrawl/
